GOLANG practice to make and office doc cracker

1.Avoid using office2john, but still log hash values
2.provide inline bruteforce and dic attack options as switches
3.inline golang to avoid dependenceies
